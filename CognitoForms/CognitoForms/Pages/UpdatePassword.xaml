<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:local="clr-namespace:SaltyDog.CognitoForms.Pages"

			 x:Class="SaltyDog.CognitoForms.UpdatePassword">
	<ContentPage.Content>
		<StackLayout Padding="32,16,32,16">
			<Label TextColor="Black" FontSize="Medium">
				<Label.Text>Please update your password.</Label.Text>
			</Label>
			<Entry x:Name="password" Placeholder="Password" Text="{Binding Password}"  HorizontalOptions="FillAndExpand" Margin="0,0,0,12" />
			<Button Text="Update" HorizontalOptions="FillAndExpand" 
					BackgroundColor="#146FE6" TextColor="White" 
					FontSize="24" HeightRequest="76" Command="{Binding CmdUpdate}" />
		</StackLayout>
	</ContentPage.Content>
</ContentPage>